{
  "name": "nekosuvai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "bench": "esbuild scripts/bench.ts --bundle --platform=node --format=cjs --outfile=node_modules/.cache/nekosuvai-bench.cjs --log-level=warning && node node_modules/.cache/nekosuvai-bench.cjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
